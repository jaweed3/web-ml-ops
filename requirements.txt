# ── Base — works on any machine (no torch, no ultralytics) ───────────────────
# Install: pip install -r requirements.txt

# ONNX inference (serving + benchmark + export tests)
onnx>=1.15.0
onnxruntime>=1.17.0

# Experiment tracking
mlflow>=2.10.0
dagshub>=0.3.0

# Config & validation
pydantic>=2.0.0
pyyaml>=6.0

# Data versioning
dvc[s3]>=3.0.0

# Image processing
opencv-python-headless>=4.9.0
numpy>=1.24.0
Pillow>=10.0.0

# Serving (Phase 2)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic-settings>=2.3.0
python-multipart>=0.0.9
prometheus-client>=0.20.0

# Research notebooks
matplotlib>=3.8.0
pandas>=2.0.0
ipykernel>=6.0.0

# Testing
pytest>=8.0.0
pytest-cov>=4.0.0
httpx>=0.27.0
