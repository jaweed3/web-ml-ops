apiVersion: v1
kind: Service
metadata:
  name: rescuevision-api
  labels:
    app: rescuevision-api
spec:
  type: ClusterIP
  selector:
    app: rescuevision-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
